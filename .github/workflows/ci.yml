name: CI

on:
  push:
    branches: [main, v4-production-architect]
  pull_request:
    branches: [main]

permissions:
  contents: read

jobs:
  test:
    runs-on: ubuntu-latest
    timeout-minutes: 15
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Setup Node
        uses: actions/setup-node@v4
        with:
          node-version: 22
          cache: npm

      - name: Install dependencies
        run: npm ci

      - name: Validate models
        run: npm run test:models

      - name: Validate Studio core
        run: npm run test:app

      - name: Validate Production Architect
        run: npm run test:production

      - name: Syntax check v4 runtime
        run: |
          node --check production-v4.js
          node --check studio-sw.js

      - name: DOM smoke test
        run: npm run test:smoke

      - name: Install Playwright Chromium
        run: npx playwright install --with-deps chromium

      - name: E2E desktop and mobile
        run: npm run test:e2e

      - name: Upload Playwright report on failure
        if: failure()
        uses: actions/upload-artifact@v4
        with:
          name: playwright-report
          path: playwright-report/
          if-no-files-found: ignore
          retention-days: 7
